{
  "name": "lifesolver-root",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "website"
  ],
  "scripts": {
    "dev:full": "npm --prefix website run dev:full",
    "dev:frontend": "npm --prefix website run dev:frontend",
    "dev:backend": "npm --prefix website run dev:backend",
    "build": "npm --prefix website run build",
    "build:full": "npm --prefix website run build"
  }
}
